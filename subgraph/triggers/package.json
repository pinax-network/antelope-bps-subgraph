{
  "name": "bps",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bps",
    "create-local": "graph create --node http://localhost:8020/ bps",
    "remove-local": "graph remove --node http://localhost:8020/ bps",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bps",
    "test": "graph test"
  },
  "dependencies": { "@graphprotocol/graph-cli": "0.78.0" }
}
