{
  "name": "antebps",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ antebps",
    "create-local": "graph create --node http://localhost:8020/ antebps",
    "remove-local": "graph remove --node http://localhost:8020/ antebps",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 antebps",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0"
  },
  "version": "v0.0.1"
}